# ZTools config skipped

Skipped ZTools application data because it contains browser storage, cookies, cache files, clipboard images, local databases, and plugin runtime state. npm-installed ZTools packages are tracked separately.

